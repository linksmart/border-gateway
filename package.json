{
  "name": "iot-bgw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "forever stopall && NODE_ENV=DEV forever run.dev.json --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "forever": "^0.15.3"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
